interface:
  display_name: "Feature Implementer"
  short_description: "Execute plans with requirement-level evidence"
  default_prompt: "Use $feature-implementer to read this planning document in full, implement only its authorized scope, track requirements and execution dependencies, and return an evidence-based integrated report."

policy:
  allow_implicit_invocation: true
